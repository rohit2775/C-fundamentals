Class

A class is a user-defined data type which acts as a blueprint for creating objects.
Jo kaam structure karta tha, wahi kaam class karega.

A class can contain:
Data Members → variables
Member Functions → functions

🔹 Object

An object is an instance of a class.
Object ka use karke hum class ke data members aur functions ko access kar sakte hain.

🔹 Difference between Structure and Class

Structure
Members are public by default.
Matlab unhe kahin se bhi access kiya jaa sakta hai.

Class
Members are private by default.
Matlab bina access specifier diye unhe kahin se access nahi kiya jaa sakta.

👉 Short Summary:

Class = Blueprint (private by default).
Object = Real entity created from class.
Structure = Members public by default.


// matlab jo kaam structure se hota tha aab wo kaam class and object se hoga .. 


example 
 

 structure man {


 };

 int main (){

    man m1 ; here  m1 is a variable 

 }


 class man (){

    man m1  // here m1 is an object 
 }









  

// ek class ke andar  member functions by default inline hote hai . 
// agar  class ke bahar function banana ho jo ki usi class ka ek entity ho  matlab member function jaisa behave kare to iss tarah se banayenge aur wo by default inline nai hoga , use inline banane ke  liye hame inline keyword ka use karna hoga ;;



 #include <iostream>
using namespace std;


 class  men
 {
    private:
    int id ;
    int regno;

    public:
     void set_name (int , int ); // step1 uss function ko class ke andar  hi declare karna hoga 

     void show_data(){
        cout<<id<<regno;
     }

 };

      void men:: set_name (int x , int y ){  // aur bahar usko define  karte waqt return type and function name ke bich me membeship level lagaya jaata hai ie. classname +::(scope resolution operator )
        id =x ;
        regno=y;
     }

 int main(){

    men m1;
    m1.set_name(123,221);
    m1.show_data();
    
    return 0;
 }
 